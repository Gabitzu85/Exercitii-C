#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    int b;
    unsigned long int c;
    printf("Dati o valoare primului numar: ");
    scanf("%d", &a);
    printf("Dati o valoare celui de-al doilea numar numar: ");
    scanf("%d", &b);
    printf("Dati o valoare celui de-al treilea numar: ");
    scanf("%d", &c);

    /* */
    return 0;
}
